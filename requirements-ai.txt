# Pile AI étendue (après requirements.txt : pypdf, chromadb, langchain-openai, etc.).
#   pip install -r requirements.txt -r requirements-ai.txt

langchain>=0.2
langchain-core>=0.2
langchain-openai>=0.1
langchain-community>=0.2
langgraph>=0.2
openai>=1.0

torch>=2.0
torchvision>=0.15
transformers>=4.40
accelerate>=0.30

# OCR sur images (PNG/JPG de politiques) — optionnel
pytesseract>=0.3.10
